@* @model IEnumerable<ReceiptsWeb.Models.Products> *@
@model PaginatedList<ReceiptsWeb.Models.Products>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<div class="modal fade" id="pricesModal" tabindex="-1" role="dialog" aria-labelledby="pricesModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content isResizable">
            <div class="modal-header draggable_touch">
                <h5 class="modal-title" id="pricesModalLabel">Prices history</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close">
                </button>
            </div>
            <div class="modal-body">
                <div id="partialPrices"></div>
            </div>
        </div>
    </div>
</div>

<p>
    <a asp-action="Create">Create New</a>
</p>

<form asp-action="Index" method="get">
    <div class="form-actions no-color">
        <p>
            Filter by group :
            <select name="filterGroup" asp-items="@ViewBag.GroupList"></select>
        </p>
        <p>
            Find by name: <input type="text" name="SearchString" value="@ViewBag.searchString" />
        </p>
        <p>
            Sort by : <select name="sort" asp-items="@ViewBag.ProductsSortList"></select>
        </p>
        <input type="submit" value="Search" />
        <a class="btn" asp-action="Index">Clear</a>
    </div>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.First().Group)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.First().Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.First().Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.First().DateReceipt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.First().SourceName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.First().SourceLine)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.First().FullData)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Group)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @item.DateReceipt.ToShortDateString()
            </td>
            <td>
                @*@Html.DisplayFor(modelItem => item.SourceName)*@
                @System.IO.Path.GetFileName(item.SourceName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SourceLine)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FullData)
            </td>
            <td>
                <nobr>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="bi bi-pen" title="Edit"></a>
                    <a asp-action="Details" asp-route-id="@item.Id" class="bi bi-info-circle" title="Details"></a>
                    <a asp-action="Delete" asp-route-id="@item.Id" class="bi bi-trash" title="Delete"></a>
                    <button class="buttonAsLink displayProductsPrices bi bi-graph-up" value="@item.Id" title="Prices"></button>
               </nobr>
            </td>
        </tr>
}
    </tbody>
</table>

@{
    var prevDisabled = !Model.HasPreviousPage ? "disabled" : "";
    var nextDisabled = !Model.HasNextPage ? "disabled" : "";
    var firstDisabled = Model.PageIndex == 1 ? "disabled" : "";
    var lastDisabled = Model.PageIndex == Model.TotalPages ? "disabled" : "";
}
@Model.PageIndex of @Model.TotalPages
<a asp-action="Index"
   asp-route-searchString="@ViewBag.searchString"
   asp-route-filterGroup="@ViewBag.filterGroup"
   asp-route-sort="@ViewBag.sort"
   asp-route-pageSize="@ViewBag.pageSize"
   asp-route-pageNumber="1"
   class="btn btn-default @firstDisabled">
    First
</a>
<a asp-action="Index"
   asp-route-searchString="@ViewBag.searchString"
   asp-route-filterGroup="@ViewBag.filterGroup"
   asp-route-sort="@ViewBag.sort"
   asp-route-pageSize="@ViewBag.pageSize"
   asp-route-pageNumber="@(Model.PageIndex - 1)"
   class="btn btn-default @prevDisabled">
    Previous
</a>
<a asp-action="Index"
   asp-route-searchString="@ViewBag.searchString"
   asp-route-filterGroup="@ViewBag.filterGroup"
   asp-route-sort="@ViewBag.sort"
   asp-route-pageSize="@ViewBag.pageSize"
   asp-route-pageNumber="@(Model.PageIndex + 1)"
   class="btn btn-default @nextDisabled">
    Next
</a>
<a asp-action="Index"
   asp-route-searchString="@ViewBag.searchString"
   asp-route-filterGroup="@ViewBag.filterGroup"
   asp-route-sort="@ViewBag.sort"
   asp-route-pageSize="@ViewBag.pageSize"
   asp-route-pageNumber="@(Model.TotalPages)"
   class="btn btn-default @lastDisabled">
    Last
</a>
Page Size : <select name="pageSize" asp-items="@ViewBag.PageSizeList" onchange="this.form.submit()"></select>
</form>

<script src="~/lib/jquery/dist/jquery.min.js"></script>
<script src="~/lib/chart/dist/js/chart.min.js"></script>

<script>
    $(document).ready(function () {
        $(".displayProductsPrices").click(function () {
            $.ajax({
                url: "/Products/GetProductPrices",
                method: "GET",
                data: { id: this.value },
                success: function (data) {
                    $("#partialPrices").html("");
                    $("#partialPrices").html(data);

                    $('#pricesModal').modal('show')
                    $(".modal-dialog").draggable({
                        cursor: "move",
                        handle: ".modal-header",
                    });
                }
            });
        });
    });
</script>